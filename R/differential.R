normalize_samples = function(lipid_measurements){
  # do normalization in here and return the df
  NULL
}

impute_missing = function(lipid_normalized){
  # do imputation
  NULL
}

differential_test = function(lipid_imputed){
  # do differential test via limma
  NULL
}